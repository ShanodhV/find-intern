import { fonts, fontWeights, fontSizes } from "./fonts";
import { colors } from "./colors";

const themeStyle = {
    colors,
    fonts,
    fontSizes,
    fontWeights,
};

export default themeStyle ;